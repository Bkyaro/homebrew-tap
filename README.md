# Bkyaro Homebrew Tap

Homebrew formulae for [Bkyaro](https://github.com/Bkyaro) projects.

## Install whatthegit

```bash
brew install Bkyaro/tap/whatthegit
```

This installs both `whatthegit` and the short alias `wtg`.
